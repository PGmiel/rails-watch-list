<%= simple_form_for(@list) do |f| %>
  <div class="form-group">
    <%= f.input :name %>
  </div>
  <div class="mt-3">
    <%= f.submit class: 'btn btn-primary' %>
    <%= link_to 'Cancel', lists_path(@lists), class: 'btn btn-danger ml-2' %>
  </div>
<% end %>
